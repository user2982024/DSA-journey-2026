#include <bits/stdc++.h>
using namespace std;

int main() {

    priority_queue<int> q;

    q.push(3);
    q.push(5);
    q.push(7);
    q.push(2);

    // q.pop();
    // q.pop();

    q.push(6);
    // q.pop();

    // cout << q.size() << "\n";

    // cout << q.top() << "\n";

    return 0;
}