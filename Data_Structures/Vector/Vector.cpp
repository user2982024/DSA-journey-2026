#include <bits/stdc++.h>
using namespace std;

int main() {
    // vector<int> v;
    // v.push_back(1);
    // v.push_back(2);
    // v.push_back(3);

    // vector<int> v = {1, 2, 3, 4, 5};

    // vector<int> v(5);
    // vector<int> v(5, 2);

    // vector<int> v = {1, 2, 3, 4, 5, 6, 7, 8};

    // for (int i = 0; i < v.size(); i ++) {
    //     cout << "Elements of vector v are: " << v[i] << "\n";
    // }
    
    // cout << "The last element of vector v is: " << v.back() << "\n";
    
    // v.pop_back();
    
    // for (int i = 0; i < v.size(); i ++) {
    //     cout << "Now the elements of vector v are: " << v[i] << "\n";
    // }
    
    // cout << "Now the last element of vector v is: " << v.back() << "\n";

    // cout << "The size of vector v is: " << v.size() << "\n";

    // v.begin() points to the first index of the vector and v.end() points to one step further to the last index of the vector.

    // vector<int> v = {2, 1, 4, 2, 5, 1};
    // sort(v.begin(), v.end());

    // for (int i = 0; i < v.size(); i ++) {
    //     cout << "Elements of vector v are: " << v[i] << "\n";
    // }
    
    // reverse(v.begin(), v.end());

    // for (int i = 0; i < v.size(); i ++) {
    //     cout << "Elements of vector v are: " << v[i] << "\n";
    // }

    vector<int> v = {1, 2, 3, 4, 5};

    cout << *v.begin() << "\n";

    return 0;
}